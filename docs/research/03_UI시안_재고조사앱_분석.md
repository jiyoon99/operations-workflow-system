stitch 폴더는 세 종류로 구성됩니다. (1) 구글 Stitch로 뽑은 정적 HTML+스크린샷 시안 6종 — 그중 _1/_2는 "OPERATIONS 운영"이라는 한국어 주문관리 보드·월간 현황 시안으로 새 시스템 UI의 직접적 레퍼런스, 나머지 4종(dashboard·inventory_list·stock_movements·settings)은 영문 재고관리(StockFlow Pro) 시안입니다. (2) 디자인 시스템 문서 2종(DESIGN.md) — 색/타이포/컴포넌트 토큰이 정리되어 있고 operations_operations_system 쪽은 에메랄드 그린 브랜드 토큰을 그대로 쓸 수 있습니다. (3) 실동작 코드 inventory_site — 무의존성 Node 서버+바닐라 JS로 만든 "실재고 조사 앱"으로, 실제 업무관리 상품 59건(노트북23·PC20 등)과 등급체계(S+S/SS/SA/AS/AA/B)가 담긴 data.json, 엑셀 임포트 도구까지 포함되어 도메인 모델 참고 가치가 가장 높습니다. 단 이 앱은 수량 기반 재고라서 자산번호(시리얼) 단위 개별 추적인 새 시스템에는 등급·카테고리 체계만 가져오고 모델은 확장이 필요합니다.

# stitch_.zip 압축해제본 내용 파악 보고

경로: `C:\Users\UserK\AppData\Local\Temp\claude\C--Users-UserK\60f8e15f-c349-40b1-8a15-ed235ad359f6\scratchpad\operations-ref\stitch\stitch_\` (총 39파일, 2.2MB)

내용물은 크게 **① Stitch 디자인 시안(정적 HTML+PNG) 6종, ② 디자인 시스템 문서 2종, ③ 실동작 재고조사 앱 1개**로 나뉩니다.

## 1. Stitch 생성 UI 시안 6종 (폴더마다 code.html + screen.png)

code.html은 전부 Tailwind CDN + Material Symbols + Inter/JetBrains Mono를 쓰는 **정적 목업**(더미데이터, 동작 로직 없음)입니다. 레이아웃/스타일 참고용이지 그대로 앱이 되는 코드는 아닙니다.

### 한국어 "OPERATIONS 운영" 시안 (★핵심 레퍼런스)
- **`_1/`** — 주문 관리 보드: 좌측 사이드바(주문 관리/월간 현황/고객 조회/시스템 설정 + "주문 수기 등록" 버튼), 상태별 KPI 카드(전체/대기 중/준비 중/제작 완료/출고 완료), 채널 뱃지(DIRECT/AMAZON), 주문번호·상품·수령인·작업상태(단계 프로그레스 점) 테이블, CSV 내보내기·일괄 작업, 처리효율 주간 바차트, 다크모드 토글. 에메랄드 그린 브랜드.
- **`_2/`** — 월간 현황: **달력형 주문 수집/출고 현황**(날짜 셀마다 건수+미니 진행바, 날짜 클릭 → 해당일 주문 리스트), 월간 총주문/선택일 주문/월간 총출고/오늘 출고 지표, 선택일 주문 테이블(주문번호 HB240504-0001 형식, 상태 뱃지: 출고 대기/결제 완료/주문 취소), Excel 내보내기.

### 영문 "StockFlow Pro / Inventory MS" 재고관리 시안
- **`dashboard/`** — 창고 개요: KPI 카드(총상품/부족알림/월간이동/저장용량 게이지), 최근 입출고 테이블(RESTOCK/OUTBOUND/INTERNAL 뱃지), 카테고리별 재고 바.
- **`inventory_list/`** — 재고 목록: 상품/SKU/카테고리/재고수준(IN STOCK·LOW STOCK·OUT OF STOCK 뱃지)/단가/최근수정 테이블, 카테고리·상태 필터, CSV 내보내기.
- **`stock_movements/`** — **입출고 감사 로그**: 일시/액션타입(STOCK IN·STOCK OUT·RETURN·ADJUST 컬러 뱃지)/SKU/수량변화(+·-)/담당자, 기간·액션·사용자 필터, 페이지네이션. → 자산 입출고 이력 화면 패턴으로 참고 가치 높음.
- **`settings/`** — 설정: 프로필/알림 토글/일반 환경설정 카드 레이아웃.

## 2. 디자인 시스템 문서 2종 (front-matter 토큰 + 가이드 산문)

- **`operations_operations_system/DESIGN.md`** — OPERATIONS 운영 시스템 디자인 토큰: primary 에메랄드 그린(#006948/#059669), Inter + 데이터용 JetBrains Mono, 4px 그리드, 240px 고정 사이드바, 그림자 대신 1px 보더+토널 레이어, 상태색 체계(대기=Slate/진행=Blue/제작=Violet/출고=Emerald/취소=Red), soft-fill 상태 칩(10% 배경+동색 텍스트), 보더-레프트 악센트 KPI 카드, sticky 헤더 테이블 등 컴포넌트 규칙까지 명문화. **새 시스템 스타일 가이드로 거의 그대로 사용 가능.**
- **`precision_logistics_system/DESIGN.md`** — Slate-900 + Blue-500 기반의 재고 시스템용 대안 토큰(영문 시안들과 짝). 구조는 동일하고 색만 다름.

## 3. `inventory_site/` — 실동작 "실재고 조사 앱" (★도메인 모델 레퍼런스)

의존성 없는 Node 앱: `server.js`(325줄, http 모듈만으로 정적 서빙 + `/api/state` GET/PUT, `/api/reset`, 회원가입/로그인/로그아웃 세션쿠키, 파일 저장) + `index.html`/`assets/app.js`(1,132줄)/`styles.css` 바닐라 SPA. 기본 포트 4173, 기본 계정 admin/1234.

- **데이터(`data.json`)가 실제 업무관리 재고**: 상품 59건 — 노트북 23, PC 20, B급 7, 업로드예정 5, 주변기기 3, 올인원 1. 각 상품에 제조사/상품코드(예: `DM500SCA_i3-10_내장`)/상품설명/판매가/노출상태와 **등급별 수량 구조 `systemGradeCounts: {S+S, SS, SA, AS, AA, B, 등급미정}`** 보유. → 업무관리의 중고 노트북 등급 체계·카테고리 체계를 여기서 그대로 확인 가능.
- **기능**: 전산재고 vs 현장 실사 수량을 등급별로 대조, 차이(diff) 자동 계산·일치 시 자동 체크, B급은 별도 카테고리(5.B급)로 분리 관리, 제조사/카테고리 필터, 엑셀 내보내기, `tools/import_inventory_excel.py`로 `재고파악.xlsx`(카테고리/상품코드/상품설명/판매가/노출상태/전산재고 컬럼) 임포트 + 자동 백업(data.backup.*.json 10개, 2026-07-08~13 작업 흔적).

## 새 시스템(업무관리 주문관리 + 자산번호 기반 노트북 매입/입출고)에 참고할 점

1. **UI 방향**: `_1`/`_2`의 OPERATIONS 한국어 시안이 곧 원하는 화면의 시각 시안 — 주문 상태 파이프라인 카드, 채널 뱃지, 월간 달력형 현황+날짜 드릴다운, 수기 등록 버튼 배치를 그대로 차용 권장. 스타일 규칙은 `operations_operations_system/DESIGN.md` 토큰을 채택하면 됨.
2. **입출고 이력 화면**: `stock_movements` 시안의 액션타입 뱃지(입고/출고/반품/조정)+수량변화+담당자+필터 구성이 자산 입출고 로그 화면의 좋은 골격.
3. **도메인 데이터**: `inventory_site/data.json`의 제조사·상품코드·등급(S+S~B·등급미정)·카테고리 체계는 실데이터 기반이므로 새 시스템의 마스터/등급 체계로 재사용 가치가 큼. 엑셀 임포트 스크립트도 초기 데이터 이관에 재활용 가능.
4. **한계 주의**: inventory_site는 "상품코드×등급별 **수량**" 재고라 개별 **자산번호(시리얼) 단위 추적이 없음**. 새 시스템은 자산 1대 단위 매입→입고→출고 이력이 핵심이므로 데이터 모델은 참고만 하고 자산 엔티티 중심으로 재설계 필요. 또 서버는 단일 JSON 파일 + 평문 인증 수준의 프로토타입이라 아키텍처 자체를 이어받을 코드는 아님.
5. 시안 code.html들은 Tailwind CDN 의존 정적 목업이므로 클래스/레이아웃만 발췌해 쓰는 것이 적절.

### 대표 파일 경로
- `...\stitch_\_1\code.html`, `...\_1\screen.png` — OPERATIONS 주문 관리 보드(한국어)
- `...\stitch_\_2\code.html` — OPERATIONS 월간 현황(달력 드릴다운)
- `...\stitch_\operations_operations_system\DESIGN.md` — 디자인 토큰(채택 권장)
- `...\stitch_\stock_movements\code.html` — 입출고 감사 로그 시안
- `...\stitch_\inventory_site\data.json` — 실제 업무관리 재고 59건·등급 체계
- `...\stitch_\inventory_site\server.js`, `assets\app.js` — 재고조사 앱 구현
- `...\stitch_\inventory_site\tools\import_inventory_excel.py` — 엑셀 재고 임포트
